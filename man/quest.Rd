% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quest.R
\name{quest}
\alias{quest}
\title{Run the QUEST pipeline}
\usage{
quest(
  x,
  dat_loc,
  nclus,
  auto_m = TRUE,
  m = 2,
  vec_m = seq(1.1, 2.5, by = 0.1),
  iter.max = 100,
  verbose = TRUE,
  reltol = 1e-04,
  initialnum = 10,
  return_all_fits = FALSE,
  uncertainty_normalized = TRUE,
  uncertainty_threshold_method = c("mean_sd", "quantile"),
  uncertainty_threshold_scale = 0.5,
  uncertainty_quantile_prob = NULL,
  run_boundary = TRUE,
  boundary_k = 10,
  run_aggregation = TRUE,
  aggregation_k = 10,
  aggregation_threshold_count = NULL,
  aggregation_threshold_prop = NULL,
  add_high_uncertain_step1 = TRUE,
  high_uncertainty_threshold_method = c("mean_sd", "quantile"),
  high_uncertainty_threshold_scale = 1.5,
  high_uncertainty_quantile_prob = NULL,
  aggregation_do_step2 = TRUE,
  aggregation_distance_threshold = 1,
  aggregation_min_component_size = 5,
  plot = FALSE
)
}
\arguments{
\item{x}{A numeric matrix of observations.}

\item{dat_loc}{A numeric matrix or data frame of spatial coordinates
with rows corresponding to observations.}

\item{nclus}{Number of clusters.}

\item{auto_m}{Logical; whether to automatically select the fuzziness
parameter using \code{fcmclust_auto()}.}

\item{m}{Fuzziness parameter used when \code{auto_m = FALSE}.}

\item{vec_m}{Candidate fuzziness parameters used when \code{auto_m = TRUE}.}

\item{iter.max}{Maximum number of iterations for each initialization.}

\item{verbose}{Logical; whether to print progress.}

\item{reltol}{Relative tolerance for convergence.}

\item{initialnum}{Number of random initializations.}

\item{return_all_fits}{Logical; whether to return all fitted models over
candidate \code{m} values when \code{auto_m = TRUE}.}

\item{uncertainty_normalized}{Logical; whether to normalize uncertainty.}

\item{uncertainty_threshold_method}{Character; one of \code{"mean_sd"} or
\code{"quantile"}.}

\item{uncertainty_threshold_scale}{Numeric; standard deviation multiplier
used when \code{uncertainty_threshold_method = "mean_sd"}.}

\item{uncertainty_quantile_prob}{Numeric; quantile probability used when
\code{uncertainty_threshold_method = "quantile"}.}

\item{run_boundary}{Logical; whether to detect boundary uncertain locations.}

\item{boundary_k}{Integer; number of nearest neighbors for boundary detection.}

\item{run_aggregation}{Logical; whether to detect aggregated uncertain locations.}

\item{aggregation_k}{Integer; number of nearest neighbors for aggregation detection.}

\item{aggregation_threshold_count}{Integer; minimum number of uncertain
neighbors required in Step 1 of aggregation detection. If \code{NULL},
\code{aggregation_threshold_prop} must be provided.}

\item{aggregation_threshold_prop}{Numeric between 0 and 1; minimum proportion
of uncertain neighbors required in Step 1 of aggregation detection.}

\item{add_high_uncertain_step1}{Logical; whether to add highly uncertain
locations in Step 1 of aggregation detection.}

\item{high_uncertainty_threshold_method}{Character; one of \code{"mean_sd"} or
\code{"quantile"} for defining highly uncertain locations used in aggregation.}

\item{high_uncertainty_threshold_scale}{Numeric; standard deviation multiplier
used when \code{high_uncertainty_threshold_method = "mean_sd"}.}

\item{high_uncertainty_quantile_prob}{Numeric; quantile probability used when
\code{high_uncertainty_threshold_method = "quantile"}.}

\item{aggregation_do_step2}{Logical; whether to run Step 2 of aggregation detection.}

\item{aggregation_distance_threshold}{Numeric; distance threshold used in
Step 3 of aggregation detection.}

\item{aggregation_min_component_size}{Integer; minimum connected component size
to keep in Step 3 of aggregation detection.}

\item{plot}{Logical; whether to generate default plots.}
}
\value{
A list with components:
\itemize{
\item clustering: Result from \code{fcmclust()} or \code{fcmclust_auto()}.
\item uncertainty: Result from \code{compute_uncertainty()}.
\item boundary: Result from \code{detect_boundary()}, or \code{NULL}.
\item aggregation: Result from \code{detect_aggregation()}, or \code{NULL}.
\item plots: A list of default plots if \code{plot = TRUE}, otherwise \code{NULL}.
\item settings: A list of key pipeline settings.
\item call: Matched function call.
}
}
\description{
Run fuzzy c-means clustering, compute uncertainty, and optionally detect
boundary and aggregated uncertain locations. Optionally generate default
visualization outputs.
}
