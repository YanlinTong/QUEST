% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quest.R
\name{quest}
\alias{quest}
\title{Run the QUEST pipeline}
\usage{
quest(
  x,
  dat_loc,
  nclus,
  auto_m = TRUE,
  m = 2,
  vec_m = seq(1.1, 2.5, by = 0.1),
  nstart = 10,
  iter_max = 100,
  reltol = 1e-04,
  verbose = FALSE,
  return_all_fits = FALSE,
  uncertainty_normalized = TRUE,
  uncertainty_threshold_method = c("mean_sd", "quantile"),
  uncertainty_threshold_scale = 0.5,
  uncertainty_quantile_prob = NULL,
  run_boundary = TRUE,
  boundary_n_neighbors = 10,
  run_aggregation = TRUE,
  aggregation_n_neighbors = 10,
  aggregation_threshold_count = NULL,
  aggregation_threshold_prop = NULL,
  add_high_uncertain_step1 = TRUE,
  high_uncertainty_threshold_method = c("mean_sd", "quantile"),
  high_uncertainty_threshold_scale = 1.5,
  high_uncertainty_quantile_prob = NULL,
  aggregation_do_step2 = TRUE,
  aggregation_distance_threshold = NULL,
  aggregation_min_component_size = NULL,
  make_plots = FALSE
)
}
\arguments{
\item{x}{A numeric matrix of dimension \eqn{n \times d}, where \eqn{n}
is the number of observations and \eqn{d} is the number of features.}

\item{dat_loc}{A numeric matrix of dimension \eqn{n \times \rho}, where
\eqn{n} is the number of observations and \eqn{\rho} is the spatial
coordinate dimension.}

\item{nclus}{An integer specifying the number of clusters.}

\item{auto_m}{A logical value indicating whether to automatically select the
fuzziness parameter using \code{fcmclust_auto()}.}

\item{m}{A numeric value greater than 1 specifying the fuzziness parameter
used when \code{auto_m = FALSE}.}

\item{vec_m}{A numeric vector specifying candidate fuzziness parameters used
when \code{auto_m = TRUE}.}

\item{nstart}{An integer specifying the number of random initializations.}

\item{iter_max}{An integer specifying the maximum number of iterations for
each initialization.}

\item{reltol}{A numeric value specifying the relative tolerance for convergence.}

\item{verbose}{A logical value indicating whether to print progress messages.}

\item{return_all_fits}{A logical value indicating whether to return all fitted
models over candidate \code{m} values when \code{auto_m = TRUE}.}

\item{uncertainty_normalized}{A logical value indicating whether to normalize
uncertainty.}

\item{uncertainty_threshold_method}{A character string specifying the threshold
method for uncertainty. Must be one of \code{"mean_sd"} or \code{"quantile"}.}

\item{uncertainty_threshold_scale}{A numeric value specifying the SD multiplier
used when \code{uncertainty_threshold_method = "mean_sd"}.}

\item{uncertainty_quantile_prob}{A numeric value between 0 and 1 specifying
the quantile probability used when
\code{uncertainty_threshold_method = "quantile"}.}

\item{run_boundary}{A logical value indicating whether to detect boundary
uncertain locations.}

\item{boundary_n_neighbors}{An integer specifying the number of nearest
neighbors for boundary detection.}

\item{run_aggregation}{A logical value indicating whether to detect aggregated
uncertain locations.}

\item{aggregation_n_neighbors}{An integer specifying the number of nearest
neighbors for aggregation detection.}

\item{aggregation_threshold_count}{An integer specifying the minimum number of
uncertain neighbors required in Step 1 of aggregation detection.}

\item{aggregation_threshold_prop}{A numeric value between 0 and 1 specifying
the minimum proportion of uncertain neighbors required in Step 1 of
aggregation detection.}

\item{add_high_uncertain_step1}{A logical value indicating whether to add
extremely uncertain locations in Step 1 of aggregation detection.}

\item{high_uncertainty_threshold_method}{A character string specifying the
threshold method for defining extremely uncertain locations used in aggregation.
Must be one of \code{"mean_sd"} or \code{"quantile"}.}

\item{high_uncertainty_threshold_scale}{A numeric value specifying the SD
multiplier used when \code{high_uncertainty_threshold_method = "mean_sd"}.}

\item{high_uncertainty_quantile_prob}{A numeric value between 0 and 1
specifying the quantile probability used when
\code{high_uncertainty_threshold_method = "quantile"}.}

\item{aggregation_do_step2}{A logical value indicating whether to run Step 2
of aggregation detection.}

\item{aggregation_distance_threshold}{A positive numeric value specifying the
distance threshold used in Step 3 of aggregation detection. If \code{NULL}, it is
automatically determined inside \code{detect_aggregation()}.}

\item{aggregation_min_component_size}{An integer specifying the minimum
connected component size used in Step 3 of aggregation detection. If \code{NULL},
it is automatically determined inside \code{detect_aggregation()}.}

\item{make_plots}{A logical value indicating whether to generate default plots.}
}
\value{
A list containing:
\itemize{
\item clustering: Result from \code{fcmclust()} or \code{fcmclust_auto()}.
\item uncertainty: Result from \code{compute_uncertainty()}.
\item boundary: Result from \code{detect_boundary()}, or \code{NULL}.
\item aggregation: Result from \code{detect_aggregation()}, or \code{NULL}.
\item plots: A list of default plots if \code{make_plots = TRUE}, otherwise \code{NULL}.
\item settings: A list of key pipeline settings.
\item call: Matched function call.
}
}
\description{
Run fuzzy c-means clustering, compute uncertainty, and optionally detect
boundary and aggregated uncertain locations. Optionally generate default
visualization outputs.
}
